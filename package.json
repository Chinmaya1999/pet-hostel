{
  "name": "wuffelune-pet-hostel",
  "version": "1.0.0",
  "private": true,
  "description": "Wuffelune — a MERN stack pet hostel (boarding) web app",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "concurrently -n api,web -c magenta,cyan \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
